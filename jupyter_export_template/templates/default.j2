<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ notebook.metadata.title or "Notebook Export" }}</title>
    <style>
        /* Default template styles */
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            line-height: 1.6;
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            background-color: #ffffff;
            color: #333333;
        }

        .notebook-header {
            border-bottom: 2px solid #e1e5e9;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }

        .notebook-title {
            font-size: 2.5em;
            font-weight: 300;
            margin: 0;
            color: #2c3e50;
        }

        .notebook-metadata {
            margin-top: 10px;
            color: #666;
            font-size: 0.9em;
        }

        .cell {
            margin-bottom: 30px;
            border-left: 4px solid transparent;
            padding-left: 20px;
        }

        .markdown-cell {
            border-left-color: #3498db;
        }

        .code-cell {
            border-left-color: #e74c3c;
        }

        .code-input {
            background-color: #f8f9fa;
            border: 1px solid #e9ecef;
            border-radius: 4px;
            padding: 15px;
            margin-bottom: 10px;
            overflow-x: auto;
        }

        .code-input pre {
            margin: 0;
            font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
            font-size: 14px;
            line-height: 1.4;
        }

        .code-input code {
            background: none;
            padding: 0;
            border: none;
        }

        .cell-output {
            background-color: #ffffff;
            border: 1px solid #dee2e6;
            border-radius: 4px;
            padding: 15px;
            margin-top: 10px;
        }

        .output-text {
            font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
            font-size: 14px;
            margin: 0;
            white-space: pre-wrap;
        }

        .output-html {
            /* Inherit styles for HTML output */
        }

        .output-image {
            max-width: 100%;
            height: auto;
            border-radius: 4px;
        }

        .error-output {
            background-color: #f8d7da;
            color: #721c24;
            padding: 15px;
            border-radius: 4px;
            font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
            font-size: 14px;
            margin: 0;
            white-space: pre-wrap;
        }

        .stream-output {
            background-color: #d4edda;
            color: #155724;
            padding: 15px;
            border-radius: 4px;
            font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
            font-size: 14px;
            margin: 0;
            white-space: pre-wrap;
        }

        /* Markdown content styles */
        .markdown-cell h1, .markdown-cell h2, .markdown-cell h3,
        .markdown-cell h4, .markdown-cell h5, .markdown-cell h6 {
            margin-top: 30px;
            margin-bottom: 15px;
            font-weight: 600;
        }

        .markdown-cell h1 { font-size: 2em; color: #2c3e50; }
        .markdown-cell h2 { font-size: 1.5em; color: #34495e; }
        .markdown-cell h3 { font-size: 1.25em; color: #34495e; }

        .markdown-cell p {
            margin-bottom: 15px;
        }

        .markdown-cell ul, .markdown-cell ol {
            margin-bottom: 15px;
            padding-left: 30px;
        }

        .markdown-cell li {
            margin-bottom: 5px;
        }

        .markdown-cell blockquote {
            border-left: 4px solid #bdc3c7;
            margin: 20px 0;
            padding-left: 20px;
            color: #666;
            font-style: italic;
        }

        .markdown-cell code {
            background-color: #f1f2f6;
            padding: 2px 6px;
            border-radius: 3px;
            font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
            font-size: 0.9em;
        }

        .markdown-cell pre {
            background-color: #f8f9fa;
            border: 1px solid #e9ecef;
            border-radius: 4px;
            padding: 15px;
            overflow-x: auto;
        }

        .unknown-output {
            background-color: #fff3cd;
            color: #856404;
            padding: 15px;
            border-radius: 4px;
            border: 1px solid #ffeaa7;
        }

        /* Responsive design */
        @media (max-width: 768px) {
            body {
                padding: 10px;
            }

            .notebook-title {
                font-size: 2em;
            }

            .cell {
                padding-left: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="notebook-header">
        <h1 class="notebook-title">{{ notebook.metadata.title or "Exported Notebook" }}</h1>
        <div class="notebook-metadata">
            {% if notebook.metadata.authors %}
                <div>Authors: {{ notebook.metadata.authors | join(", ") }}</div>
            {% endif %}
            {% if notebook.metadata.date %}
                <div>Date: {{ notebook.metadata.date }}</div>
            {% endif %}
        </div>
    </div>

    <div class="notebook-content">
        {% for cell in notebook.cells %}
            {% if cell.cell_type == 'markdown' %}
                <div class="cell markdown-cell" id="{{ cell | cell_id }}">
                    {{ cell.source | markdown | safe }}
                </div>
            {% elif cell.cell_type == 'code' %}
                <div class="cell code-cell" id="{{ cell | cell_id }}">
                    <div class="code-input">
                        <pre><code>{{ cell.source }}</code></pre>
                    </div>
                    {% if cell.outputs %}
                        {% for output in cell.outputs %}
                            <div class="cell-output">
                                {{ output | render_output | safe }}
                            </div>
                        {% endfor %}
                    {% endif %}
                </div>
            {% elif cell.cell_type == 'raw' %}
                <div class="cell raw-cell" id="{{ cell | cell_id }}">
                    <pre>{{ cell.source }}</pre>
                </div>
            {% endif %}
        {% endfor %}
    </div>
</body>
</html>
